sonar.projectKey=huzaifa-fullstack_huzaifakarim-mern-10pshine
sonar.organization=huzaifa-fullstack

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=Scribo - AI-Powered Smart Notes
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=backend/src,frontend/src

# Test files location
sonar.tests=backend/tests,frontend/src/tests

# Exclusions (exclude from analysis entirely)
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/coverage/**,**/.nyc_output/**,**/public/**,**/*.d.ts,**/*.config.js,**/*.config.ts,**/vite.config.*,**/vitest.config.*,**/eslint.config.*,frontend/src/tests/**

# Test exclusions (for source files only)
sonar.test.exclusions=**/*.test.js,**/*.test.ts,**/*.test.tsx,**/*.spec.js,**/*.spec.ts,**/*.spec.tsx

# Coverage reports
sonar.javascript.lcov.reportPaths=backend/coverage/lcov.info,frontend/coverage/lcov.info
sonar.typescript.lcov.reportPaths=backend/coverage/lcov.info,frontend/coverage/lcov.info

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
